# Frontend ↔ Backend API Reconciliation

This document reconciles frontend API expectations against the backend freeze preparation scope.

> Note: In this repository snapshot, the following backend freeze documents were not present at the expected paths and could not be line-by-line validated directly:
> - `docs/api-freeze-endpoint-inventory.md`
> - `docs/frontend-backend-integration-contract.md`
> - `docs/permissions-map.md`
> - `docs/lookups-contract.md`
> - `docs/backend-api-freeze-readiness-matrix.md`
>
> The table below is the final reconciliation baseline for integration planning, using the existing frontend readiness matrix and the freeze scope decisions. Any row marked `decision_needed` or `backend_partial` requires a final confirmation pass once the backend freeze documents are synced into this repository.

## Reconciliation matrix

| module | frontend route | frontend mock service | expected endpoint | backend endpoint status | permissions status | lookups status | response compatibility | integration status | notes |
|---|---|---|---|---|---|---|---|---|---|
| auth | `/login` | `auth.mock.service.ts` | `POST /api/tenant/login` | expected in freeze scope | auth permissions payload expected from login/me | none | compatible with `ApiSuccess<ApiSession>` expectation | ready_to_integrate | Integrate first with session bootstrap and token/workspace persistence. |
| lookups | shared (no dedicated route) | currently in per-feature mocks (no unified lookup service) | `GET /api/tenant/lookups/*` | partially defined by module assumptions; freeze doc confirmation needed | permission-gated by consuming modules | required across customers/dresses/invoices/payments/etc. | compatible if normalized to shared option shape `{label,value}` | frontend_adjustment_needed | Add centralized lookup wiring/caching strategy before module-by-module real API swaps. |
| dashboard | `/dashboard` | `dashboard.mock.service.ts` | `GET /api/tenant/dashboard/overview` | deferred for phase start | `dashboard.view` mapping not finalized in this snapshot | branch/department filters expected | KPI payload not yet contract-validated in this snapshot | backend_deferred | Keep on mocks until deferred analytics scope is unblocked. |
| customers | `/customers` | `customers.mock.service.ts` | `GET/POST/PUT/DELETE /api/tenant/customers` | endpoint family assumed available in freeze scope | `customers.view` and CRUD actions expected | city/status/source lookups required | compatible with shared paginated table envelope | ready_to_integrate | First CRUD business module after auth/lookups. |
| categories | `/categories` | `categories.mock.service.ts` | `GET/POST/PUT/DELETE /api/tenant/categories` | endpoint family assumed available in freeze scope | `dress_categories.view` expected | status lookups expected | compatible with shared list + mutation envelope | ready_to_integrate | Pair with subcategories in the same rollout wave. |
| subcategories | `/subcategories` | `subcategories.mock.service.ts` | `GET/POST/PUT/DELETE /api/tenant/subcategories` | endpoint family assumed available in freeze scope | `dress_categories.view` expected | depends on categories lookup | compatible with shared list + mutation envelope | ready_to_integrate | Requires categories lookup preloading for create/edit forms. |
| branches | `/branches` | `branches.mock.service.ts` | `GET/POST/PUT/DELETE /api/tenant/branches` | endpoint family assumed available in freeze scope | `branches.view` expected | currency/city/status lookups expected | compatible if VAT/tax fields follow frozen schema | ready_to_integrate | Keep branch entity as prerequisite for downstream filters. |
| dresses | `/dresses` | `dresses.mock.service.ts` | `GET/POST/PUT/DELETE /api/tenant/dresses` | endpoint family assumed available in freeze scope | `dresses.view` expected | branches/categories/status lookups required | compatible with paginated list + mutation responses | ready_to_integrate | Depends on lookups + categories + branches readiness. |
| invoices | `/invoices` | `invoices.mock.service.ts` | `GET /api/tenant/invoices` (+ actions) | listing endpoint assumed available; actions require confirmation | `invoices.view` expected | customers/types/status/branches lookups required | listing compatible; action envelopes need confirmation | ready_to_integrate | Start with list/read integration, then incrementally enable actions. |
| payments | `/payments` | `payments.mock.service.ts` | `GET/POST /api/tenant/payments`, `POST /{id}/pay`, `POST /{id}/cancel` | core endpoints assumed available; action/export details need final verification | `payments.view` expected | branch/customer/type/status lookups required | compatible for list/create; lifecycle action payloads need confirmation | backend_partial | Integrate list/create first, then pay/cancel/export as second step. |
| deliveries | `/deliveries` | `deliveries.mock.service.ts` | `GET /api/tenant/deliveries`, `POST /{id}/deliver` | endpoint family assumed available in freeze scope | `invoice_delivery.view` expected | customer/employee/date filters required | compatible for list + action pattern | ready_to_integrate | Deliver action error/validation messages must map to shared toast format. |
| returns | `/returns` | `returns.mock.service.ts` | `GET /api/tenant/returns`, `POST /{id}/return` | endpoint family assumed available in freeze scope | `invoice_delivery.view` expected | customer/employee/date filters required | compatible for list + action pattern | ready_to_integrate | Keep aligned with deliveries integration to avoid duplicate adapters. |
| overdue returns | `/overdue-returns` | `returns.mock.service.ts` | `GET /api/tenant/returns/overdue` | overdue aggregation contract needs final confirmation | `invoice_delivery.view` expected | customer + overdue range filters required | may need pagination/meta adapter based on backend calculation endpoint | backend_partial | Integrate after base returns endpoints stabilize. |
| expenses | `/expenses` | `expenses.mock.service.ts` | `GET/POST/PUT/DELETE /api/tenant/expenses` + approve/pay/cancel actions | core endpoints assumed available; action payload details need final check | `expenses.view` expected | branches/cashboxes/categories/status lookups required | list/create/update compatible; action envelope needs confirmation | ready_to_integrate | Introduce actions progressively after base CRUD integration. |
| cash movements | `/cash-movements` | `cash-movements.mock.service.ts` | `GET /api/tenant/cash-movements` | list endpoint assumed available in freeze scope | `cash_movements.view` expected | cashbox/type lookups required | compatible with shared paginated table envelope | ready_to_integrate | Good candidate immediately after expenses. |
| cashboxes | `/cashboxes` | `cashboxes.mock.service.ts` | `GET/PUT /api/tenant/cashboxes`, recalculate/export endpoints | base endpoints assumed available; recalculate/export confirmation pending | `cashboxes.view` expected | branch/status lookups required | list/update compatible; recalculate/export behavior needs final contract check | backend_partial | Add export/download helper before enabling export endpoint. |
| suppliers | `/suppliers` | `suppliers.mock.service.ts` | `GET/POST/PUT/DELETE /api/tenant/suppliers` | endpoint family assumed available in freeze scope | `suppliers.view` expected | supplier status/branch lookups required | compatible with shared list + mutation envelope | ready_to_integrate | Prerequisite module for purchase orders and supplier payments. |
| purchase orders | `/purchase-orders` | `suppliers.mock.service.ts` | `GET/POST /api/tenant/purchase-orders` + add-payment/return/export | core endpoints assumed available; action/export details need final verification | `purchase_orders.view` expected | supplier/status/date lookups required | list/create compatible; lifecycle/export envelopes need confirmation | backend_partial | Integrate in two steps: base PO first, then advanced actions/export. |
| supplier payments | `/supplier-payments` | `suppliers.mock.service.ts` | `GET/POST /api/tenant/supplier-payments` + export | core endpoints assumed available; export confirmation pending | `supplier_payments.view` expected | supplier/PO/method lookups required | list/create compatible; export file response contract needs confirmation | backend_partial | Share export/download utility with cashboxes and purchase orders. |
| reports | `/reports`, `/reports/sales`, `/reports/tailoring` | `reports.mock.service.ts` | `GET /api/tenant/reports/overview`, `/sales`, `/tailoring` | deferred for post-core integration phase | `reports.*` permissions not validated in this snapshot | branch/employee/date lookups required | analytics payload shape not validated in this snapshot | backend_deferred | Keep mock-only until reporting contracts are finalized. |
| accounting | `/accounting` | `accounting.mock.service.ts` | `GET /api/tenant/accounting/summary`, `GET /api/tenant/accounting/ledger` | deferred for post-core integration phase | `accounting.view` mapping not validated in this snapshot | cashbox/type/date lookups required | ledger/summary schema not validated in this snapshot | backend_deferred | Integrate after operational modules are stable. |
| settings | `/settings/account` | `settings.mock.service.ts` | `GET/PUT /api/tenant/settings/account`, password/change + delete-account | account endpoint details require final freeze confirmation | `settings.profile` expected; broader settings permissions deferred | minimal lookup dependency | basic profile response likely compatible; advanced account workflows need confirmation | backend_deferred | Only account basics are in current UI scope; full settings management remains deferred. |

## Reconciliation summary

- **ready_to_integrate (baseline):** auth, customers, categories, subcategories, branches, dresses, invoices, deliveries, returns, expenses, cash movements, suppliers.
- **backend_partial:** payments, overdue returns, cashboxes, purchase orders, supplier payments.
- **backend_deferred:** dashboard, reports, accounting, settings.
- **frontend_adjustment_needed:** lookups (shared lookup strategy is required before broad real API activation).
- **decision_needed:** none currently blocked in table status, but final backend freeze doc sync is still required for `backend_partial` rows before action/export activation.
