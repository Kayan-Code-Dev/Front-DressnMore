import AccountSettings from "@/pages/account/AccountSettings";

/** تبويب البروفايل داخل صفحة الإعدادات — نفس منطق `/account` */
export default function SettingsProfileTab() {
  return <AccountSettings embedded />;
}
